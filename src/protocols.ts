export type ApplicationError = {
    name: string;
    message: string;
}

export type User = {
    cpf: string;
    name: string;
    email: string;
    password: string;
}

export type Company = {
    cnpj: string
    name: string
    email: string
    password: string
  }
